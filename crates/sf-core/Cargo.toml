[package]
name = "sf-core"
description = "SentenceFlow pure-logic core: LevelSpec interpretation, SRS, session building, judging, stats folding. No IO, no platform dependencies — compiled identically for desktop (native) and web trial (wasm)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
