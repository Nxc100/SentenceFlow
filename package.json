{
  "name": "sentenceflow-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "句流 SentenceFlow — 买断制英语整句打字训练软件 (monorepo)",
  "workspaces": [
    "packages/*",
    "apps/web-trial",
    "apps/desktop"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build crates/sf-wasm --target web --out-dir pkg && node scripts/copy-wasm.mjs",
    "dev:trial": "npm run dev --workspace=web-trial",
    "build:trial": "npm run build --workspace=web-trial",
    "typecheck": "tsc --noEmit -p packages/ui && tsc --noEmit -p apps/web-trial && tsc --noEmit -p apps/desktop"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wasm-pack": "^0.13.1"
  }
}
