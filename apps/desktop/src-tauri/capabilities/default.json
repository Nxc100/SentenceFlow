{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window baseline capability — core events only; every feature goes through explicit commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
