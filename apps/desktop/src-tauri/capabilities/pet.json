{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "宠物窗:最小权限。只额外放行前端确实要调的两个窗口写操作 —— 漫游/拖拽落位(set-position)与轮廓级点击穿透(set-ignore-cursor-events)。读位置/尺寸/显示器已含在 core:default 里;显隐、缩放、置顶等一律走显式 pet_* 命令,不给前端直通权限。",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position",
    "core:window:allow-set-ignore-cursor-events"
  ]
}
