# 开发状态与偏差记录

> 基准:《句流-完整开发文档-v5-合订版》(2026-08-16)。
> 本文件记录当前实现覆盖面、与规范的**有意偏差**及其理由、尚待完成项。
> 更新时间:2026-08-17。

## 0. 2026-08-17 交互修订(对齐参照原型 example/句子打字练习.html)

- **空态修复**:练习页状态分支重排 —— 空队列不再落在无出口的空白加载页;
  任何加载/空态都常驻顶栏返回;今日页在无句可练时禁用入口并给指引;
  无标注导入句打字改用分词兜底、完成后跳过(无内容可展示的)解析页。
- **解析页键盘流**:空格/Enter 下一题(动效定格后生效;跳过动效的那次按键
  不透传)、←→ 朗读整句;底栏常驻 ‹上一题/下一题› + 随视图切换的快捷键条。
- **打字细节**:词满后续键自动落入下一未完成词;点击词格定位;↓ 改为格内
  浅灰揭示;Enter 未完成时定位第一处问题;Ctrl 朗读改 keyup 无组合触发
  (修 Ctrl+M 误朗读)。
- **音效落地**(§6.5,WebAudio 合成,无资源文件):按键三音色 ±3% 音高、
  正确叮 880Hz/90ms/−14dB、错误低音 220Hz 更轻、掌握上行双音;
  随设置(音色/音量)实时生效;`packages/ui/src/sounds.ts`。
- **解析视图**:音标加 /斜杠/;"句子解析"改卡片盒 + 成分色句型 chips。

### AI 答疑体验修订(同日第二轮,打包 exe 实测)

- **子进程静默**:Windows 下 GUI 进程 spawn `opencode.cmd` 会弹黑色控制台;
  所有 opencode/piper spawn 点统一加 `CREATE_NO_WINDOW`(实测请求期间
  可见控制台窗口数为 0)。`sf-llm/channels/opencode.rs` `hidden_command`。
- **会话记录**:答疑抽屉改聊天气泡流 —— 同一句内多轮累积,近 3 轮随新问题
  回传作上下文(`ask_ai` 增加 history 参数);进入下一题自动清空;
  Esc 优先关抽屉再谈暂停。
- **打字机流式**:opencode run 的 text 事件整段到达,前端以 60 字/秒缓冲
  匀速吐字(实测 ≈57 字/秒)+ 闪烁光标,对 SSE 细粒度通道同样适用。
- **Markdown 排版**:新增共享轻量渲染组件(段落/列表/加粗/行内代码,
  纯 React 节点无注入面,不引第三方库)`packages/ui/components/Markdown.tsx`;
  答疑与周点评共用。
- **抽屉拖拽调高**:顶部拖拽条,240px–85vh,记忆到 localStorage。

### 网络与答案显示修订(同日第三轮,打包 exe 实测)

- **AI 代理设置**(直连网络访问境外端点):设置 · AI 接入新增"网络"区,
  可填本机代理客户端 HTTP 端口(如 `http://127.0.0.1:7897`),仅 AI 请求
  走代理 —— opencode 子进程经 HTTP(S)_PROXY 环境变量注入(Bun fetch 遵守,
  NO_PROXY 排除 localhost),DeepSeek/Zen 经 reqwest Proxy;Ollama 本地
  强制 no_proxy(环境代理开着也不受影响);留空 = 跟随系统。
  CLI 侧对应 `SF_PROXY` 环境变量。实测:坏端口 → 请求失败(证明传导),
  真实本地代理端口 → 答疑成功。
- **答案显示**:↓ 揭示改为底部一行品牌蓝 13px 小字(替代格内浅灰填充),
  ↑ 收起;seen_answer 语义不变。
- **模型可达性标注**(面向国内用户):channels.json 新增
  `proxy_required_models` 名单(用户真机直连实测 2026-08-17:Zen 网关国内
  可达,仅 deepseek-v4-flash 系上游需代理,其余 6 个免费模型直连可用)。
  探测时由编排层按名单给模型打标(适配器不感知策略),下拉逐项标注
  「🔒 需代理 / · 直连可用」;未配代理选中需代理模型给内联警告;
  默认选择与微基准均优先/仅取直连可用模型。channels.json 加载补上
  随包资源一级回退(此前只读 app-data 副本)。名单变化随内容包下发,零代码。

## 一、里程碑对照(§9)

| 周 | 目标 | 状态 |
|---|---|---|
| W1 | LevelSpec 定稿、金标、校验器 | ✅ 六级 YAML + 校验器全量单测 + `sf gold run` 100% |
| W2 | opencode 适配 spike | ✅ **真机完成**(opencode 1.18.18,2026-08-16):三态探测/免费模型枚举/沙箱 run/流式生成/答疑全链路在打包应用里跑通。钉死结论见"三、W2 真机 spike 结论" |
| W3 | sf-llm 四通道 + 计量双轨 + channels.json;管线双档 | ✅ |
| W4–5 | sf-core + Tauri 壳 + UI 包:打字/重组/解析/听打/默写 | ✅ 双端(桌面 + 试用站)同一 wasm/native 核心;试用站已浏览器实测 |
| W6 | 全量生产 5000 句、抽审 | ◐ 管线与 CLI 就绪;种子 50 句 + NGSL 全量词典(2827 词条)已入库;**5000 句跑批待执行**(通道已验证可用) |
| W7 | 生成工坊全交互 | ✅ 队列/续跑/停止/微批/修补调用/微基准/四态通道卡(断网续跑演练待真机) |
| W8 | 授权体系、报告 + 周报 PDF | ✅ 签发 CLI/激活/试用/体验模式/回拨检测;周报走系统打印→PDF |
| W9 | 深色/护眼、备份恢复、Web 试用站 | ✅(内容包后台更新通道未做,见下) |
| W10 | 签名公证、发卡接入、alpha | ✗ 商务/证书类事项,代码侧无法完成 |

## 二、与规范的有意偏差(均为实现取舍,已在代码注释中就地记录)

1. **token 预估不用 tiktoken-rs**(§7.5)——四通道没有一家按 tiktoken 词表计费,
   精确的 tiktoken 数一样是错的;改用字符类启发式(刻意偏高保守),
   实际用量以 API usage 回填为准,UI 恒标"以官方账单为准"。`sf-llm/src/estimate.rs`
2. **simhash 阈值 16、bigram 权重 1**(§7.4)——按种子句库实测标定:
   真近重复 6–16,不同短句最低 17;宁漏勿错杀(漏网靠抽审)。`sf-pipeline/src/simhash.rs`
3. **content.db 无 note_tpl/note_slots 表**(§7.7)——note 模板化是工厂侧省
   token 手段,库内保留反范式 `note` 单列,模板层留在 prompt 侧。`sf-pipeline/src/store.rs`
4. **progress.db 新增 `favorite` 表**(§7.7 未列)——收藏(§4.3)需要落地处;
   错题本仍按规范由 `srs` 派生。`apps/desktop/src-tauri/src/progress.rs`
5. **opencode 只用 standalone `run`,不用 serve/attach**(§3.4)——真机 spike
   证实 1.18.18 的 `run --attach` 对带密码 serve 只回吐 step_start、不转发
   text 事件,serve 优先策略在当前 CLI 面上不可实现;按 §10 逃生门取文档
   规定的降级路径(每微批冷启动 ~1-2s)。重返 serve 需走官方
   `@opencode-ai/sdk` 会话 API。
6. **周报 PDF = 打印样式 + 系统"另存为 PDF"**(§4.5)——避免引入 PDF 生成
   依赖;报告页带 `@media print` 版式。
7. **词表带用 HashMap 而非 fst**(§7.4"NGSL fst")——2800 词条规模下 fst
   无收益;查询接口不变,后续可无痛替换。
8. **重组通过后进入解析视图无独立"mini 签名时刻"**——重组定位为入口玩法,
   直接衔接打字二遍流;撒花仍为打字答对专属。

## 三、W2 真机 spike 结论(opencode 1.18.18,Windows,2026-08-16)

- `opencode run -m <model> --format json`:每行一个 JSON 事件——
  `{"type":"step_start"…}` / `{"type":"text","part":{"text":"…"}}` /
  `{"type":"step_finish",…,"tokens":{…},"cost":0}`;prompt 经 stdin 正常;
  沙箱 cwd + 我方 `opencode.json`(agent.sf-gen.permission 全 deny 键形)不报错。
- `opencode serve --port 0` **落在默认端口 4096 而非随机端口**;URL 打到
  stdout:"opencode server listening on http://127.0.0.1:4096"。
- `run --attach`:无密码报 "Session not found";带同密码环境变量能连上,
  但只转发 step_start、**text 事件不回吐** → attach 不可用作补全传输,已从
  适配器移除(见偏差 5)。
- npm 全局安装形态为 `opencode.cmd`,tokio spawn 直接可用(探测与 run 均验证)。
- 免费模型名单(时点):big-pickle、deepseek-v4-flash-free、hy3-free、
  laguna-s-2.1-free、mimo-v2.5-free、nemotron-3-ultra-free、
  nemotron-3.5-lightning-free。deepseek-v4-flash-free 的 JSON 纪律:
  两轮 10 句批全部可解析(破碎数据 0)。

## 四、待完成(按优先级)

1. **全量内容生产**:种子句扩到金标 200,跑批到 L1–L6 共 5000 句,抽审 5%
   (通道与词典已就绪;L2 批实测通过率 3/10,多数丢弃是 NGSL 外词——出厂
   生产建议 L3+ 或在 prompt 中强化词表约束)。
2. **词典 IPA/释义补全**:NGSL 全量 2801 词已入库(band=官方词频 rank),
   但仅种子句相关 144 词带 IPA/释义;词典对账仅在有值时覆写,其余词条
   IPA 依赖模型输出。
3. **音频资产**:piper 声音包(en_GB-alba / en_US-lessac)+ 词级 mp3 包;
   sidecar 调用与缓存已实现(`tts.rs`),缺声学模型文件;当前回退 WebView
   speechSynthesis。按键音/正确音/错误音的音频文件同理(前端事件位已留)。
4. **内容包更新通道**(§7.8):Tauri updater 配置 + content.db rev 静默下载
   与原子替换;channels.json 的应用侧热加载已支持(启动时读 app-data 副本)。
5. **打包发布**:三平台图标(现为占位)、NSIS/dmg/AppImage bundle 配置、
   代码签名/公证、发卡平台接入、试用站部署(Cloudflare Pages)。
6. **细节打磨**:听打 Ctrl 重听计数(≥3 记听力薄弱信号)、长按 0.75× 慢速、
   90 秒无输入提示、字体内置(Nunito/Charis SIL IPA 子集/OpenDyslexic 的
   woff2 打包)、生成句卡 hover [重新生成] 的单卡重生成。
7. **发布前必做**:更换 `LICENSE_PUBLIC_KEY_B64` 为生产公钥
   (tools/dev-keys 仅供本地端到端测试)。

## 五、已验证

- **打包应用 UI 全量实测**(2026-08-16,Playwright 经 WebView2 CDP 驱动
  release 版 exe):首启定级(卡片 + 三问测验)/严格与宽松打字/防双跳/
  错字不上屏/答案显隐/签名时刻(成分卡+音标+胶囊+撒花)/Ctrl+M/Ctrl+Q/
  Esc 暂停/内容库四 tab/收藏/错题本/双列导入与删除/四通道三态探测/
  opencode 选择+模型下拉+条款/**opencode 真机生成入库(3 句过检,7 句
  合理越级丢弃可捞回)**/AI 答疑流式回答/授权激活(错误抖动+盖章+掩码)/
  试用胶囊/备份导出(zip 含双库)/报告(今日卡+薄弱分析)/深色 #101114 与
  纸色 #F7F2E7。修复的 4 个真机 bug:CSP 拦 IPC fetch、attach 密码/事件
  缺陷(移除 attach)、备份相对路径落 exe 目录、页面级 CSS 误用原始灰阶
  令牌致深色/纸色失效。
- `cargo test --workspace --all-features`:**146 用例全绿**(六 crate + 桌面壳)。
- `cargo fmt --check` + `cargo clippy -D warnings`(四核心 crate):干净。
- `sf factory build`:种子 50/50 过校验入库;`sf gold run` 100%。
- Web 试用版:vite build 通过;Playwright 实测「首页 → L1 → 拆句重组 →
  整句打字 → 签名时刻(成分卡/音标/词性胶囊)」全链路,wasm 引擎(非 mock)。
- 桌面端:`cargo build` 无警告,25 秒启动冒烟无 panic;前端 typecheck/build 通过。
